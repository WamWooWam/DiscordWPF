<UserControl x:Class="DiscordWPF.Controls.LoadingControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DiscordWPF.Controls"
             mc:Ignorable="d" Foreground="{DynamicResource SystemBaseHighBrush}">

    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Stretch" Margin="20">
        <Image Margin="10" MaxWidth="350" Source="{StaticResource DiscordWordMark}"/>
        <ProgressBar x:Name="connetingProgress" IsIndeterminate="True" MaxWidth="350" Height="8" Background="Transparent" Margin="20,10"/>
        <TextBlock x:Name="connectingStatus" HorizontalAlignment="Center" FontSize="14" Text="Checking for Updates"/>
    </StackPanel>
</UserControl>
